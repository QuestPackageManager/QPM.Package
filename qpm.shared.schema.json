{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SharedPackageConfig",
  "description": "Configuration for a shared package.",
  "type": "object",
  "required": [
    "config",
    "env",
    "restoredDependencies"
  ],
  "properties": {
    "config": {
      "description": "Package name",
      "allOf": [
        {
          "$ref": "#/definitions/PackageConfig"
        }
      ]
    },
    "env": {
      "description": "Restored environment variables",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "restoredDependencies": {
      "description": "Restored dependencies",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/SharedDependencyInfo"
      }
    }
  },
  "definitions": {
    "DependencyId": {
      "type": "string"
    },
    "PackageAdditionalData": {
      "type": "object",
      "properties": {
        "author": {
          "description": "Package author",
          "default": "",
          "type": "string"
        },
        "description": {
          "description": "Package description",
          "default": "",
          "type": "string"
        },
        "license": {
          "description": "Package license",
          "default": "",
          "type": "string"
        },
        "url": {
          "description": "General URL for the mod.",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "PackageCompileOptions": {
      "description": "Additional options for compilation and edits to compilation related files.",
      "type": "object",
      "properties": {
        "cFlags": {
          "description": "Additional C flags to add.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "cppFlags": {
          "description": "Additional C++ flags to add.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "includePaths": {
          "description": "Additional include paths to add, relative to the extern package's shared directory.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "systemIncludes": {
          "description": "Additional system include paths to add, relative to the extern package's shared directory.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        }
      }
    },
    "PackageConfig": {
      "description": "Configuration for a package.",
      "type": "object",
      "required": [
        "dependenciesDirectory",
        "id",
        "sharedDirectory",
        "version"
      ],
      "properties": {
        "additionalData": {
          "description": "Additional package metadata",
          "default": {
            "author": "",
            "description": "",
            "license": ""
          },
          "allOf": [
            {
              "$ref": "#/definitions/PackageAdditionalData"
            }
          ]
        },
        "compileOptions": {
          "description": "Additional compile options for the package.",
          "anyOf": [
            {
              "$ref": "#/definitions/PackageCompileOptions"
            },
            {
              "type": "null"
            }
          ]
        },
        "configVersion": {
          "description": "Config version, defaults to 2.0.0",
          "default": "2.0.0",
          "type": "string",
          "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-((?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\\.(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\\+([0-9a-zA-Z-]+(?:\\.[0-9a-zA-Z-]+)*))?$"
        },
        "dependencies": {
          "description": "Dependencies for this package",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/PackageDependency"
          }
        },
        "dependenciesDirectory": {
          "description": "Directory where dependencies are restored",
          "type": "string"
        },
        "devDependencies": {
          "description": "Dev dependencies for this package",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/PackageDependency"
          }
        },
        "id": {
          "description": "Package ID",
          "allOf": [
            {
              "$ref": "#/definitions/DependencyId"
            }
          ]
        },
        "qmod": {
          "description": "QMod configuration",
          "allOf": [
            {
              "$ref": "#/definitions/QmodConfig"
            }
          ]
        },
        "sharedDirectory": {
          "description": "Directories shared by the package",
          "type": "string"
        },
        "version": {
          "description": "Package version",
          "type": "string",
          "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-((?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\\.(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\\+([0-9a-zA-Z-]+(?:\\.[0-9a-zA-Z-]+)*))?$"
        },
        "workspace": {
          "description": "Workspace configuration",
          "default": {
            "scripts": {}
          },
          "allOf": [
            {
              "$ref": "#/definitions/WorkspaceConfig"
            }
          ]
        }
      }
    },
    "PackageDependency": {
      "description": "Dependency information for a package.",
      "type": "object",
      "required": [
        "versionRange"
      ],
      "properties": {
        "qmod": {
          "description": "Whether to include this dependency in the qmod",
          "anyOf": [
            {
              "$ref": "#/definitions/QmodDependencyMode"
            },
            {
              "type": "null"
            }
          ]
        },
        "qpkgUrl": {
          "description": "The URL to download the dependency from. Useful for nightly/bleeding edge usage.",
          "type": [
            "string",
            "null"
          ]
        },
        "versionRange": {
          "description": "Version range requirement",
          "type": "string",
          "properties": {
            "format": {
              "title": "String",
              "type": "string"
            }
          }
        }
      }
    },
    "QmodConfig": {
      "description": "QMod package configuration.",
      "type": "object",
      "properties": {
        "downloadUrl": {
          "description": "Download URL used in the generated mod.json.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "QMod ID for this package.",
          "type": [
            "string",
            "null"
          ]
        },
        "includeFiles": {
          "description": "Files to include in the resulting qmod.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "output": {
          "description": "The output name of the QMOD, default to something based on the package ID.",
          "type": [
            "string",
            "null"
          ]
        },
        "searchDirs": {
          "description": "Directories to search during qmod creation.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "template": {
          "description": "The template path for the mod.json, default to mod.template.json.",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "QmodDependencyMode": {
      "description": "QMod dependency flag",
      "oneOf": [
        {
          "description": "Do not include the dependency in the resulting QMod",
          "type": "string",
          "enum": [
            "none"
          ]
        },
        {
          "description": "The dependency is required for the QMod",
          "type": "string",
          "enum": [
            "required"
          ]
        },
        {
          "description": "The dependency is optional for the QMod",
          "type": "string",
          "enum": [
            "optional"
          ]
        }
      ]
    },
    "SharedDependencyInfo": {
      "description": "Dependency information for a shared package.",
      "type": "object",
      "required": [
        "restoredBinaries",
        "restoredEnv",
        "restoredVersion"
      ],
      "properties": {
        "qpkgChecksum": {
          "description": "The sha256 checksum of the restored QPKG's content, if known. Lets a restore recognize an already-cached copy without re-downloading it, and detect when the content behind a dependency has changed since it was last resolved.",
          "type": [
            "string",
            "null"
          ]
        },
        "qpkgUrl": {
          "description": "The URL to download the dependency from. Useful for nightly/bleeding edge usage.",
          "type": [
            "string",
            "null"
          ]
        },
        "restoredBinaries": {
          "description": "Binaries for this dependency.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "restoredEnv": {
          "description": "Restored environment variables for the dependency.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "restoredVersion": {
          "description": "Version of the dependency.",
          "type": "string",
          "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-((?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\\.(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\\+([0-9a-zA-Z-]+(?:\\.[0-9a-zA-Z-]+)*))?$"
        }
      }
    },
    "WorkspaceConfig": {
      "description": "Configuration for the workspace.",
      "type": "object",
      "required": [
        "ndk"
      ],
      "properties": {
        "cmake": {
          "description": "Whether to generate CMake files on restore.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "env": {
          "description": "Environment variables for the workspace.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "ndk": {
          "description": "NDK version range requirement.",
          "type": "string",
          "properties": {
            "format": {
              "title": "String",
              "type": "string"
            }
          }
        },
        "outBinaries": {
          "description": "Output binaries for this package.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "scripts": {
          "description": "Scripts associated with the workspace.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "toolchainOut": {
          "description": "Path to generate a toolchain JSON file describing the project setup configuration.",
          "type": [
            "string",
            "null"
          ]
        }
      }
    }
  }
}